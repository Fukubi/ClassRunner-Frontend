# Web Frontend for ClassRunner

Web Frontend for the ClassRunner written in Javascript using Vite, React and Typescript.

Usage:

npm:
```
npm install
npm run dev
``` 

yarn:
```
yarn install
yarn dev
```